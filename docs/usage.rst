Usage
=====

Here's an example of how to use Spin Sampler:

.. code-block:: python

    from spin_sampler import Sampler

    sampler = Sampler()
    result = sampler.sample()
    print(result)

For more details, see the API reference.